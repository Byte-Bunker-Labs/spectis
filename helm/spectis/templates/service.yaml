apiVersion: v1
kind: Service
metadata:
  name: {{ include "spectis.fullname" . }}
  labels:
    {{- include "spectis.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "spectis.selectorLabels" . | nindent 4 }}
